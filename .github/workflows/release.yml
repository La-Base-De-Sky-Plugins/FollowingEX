name: Create release

on:
  release:
    types:
      - created

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Zip Folder
      run: zip -r "LBDSKY_FollowingEX.zip" . -i "Graphics/*" "Plugins/*" "Data/*"

    - name: Release
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: "LBDSKY_FollowingEX.zip"
        generate_release_notes: true
        make_latest: true
        token: ${{ secrets.RELEASE_TOKEN_PLUGINS }}
